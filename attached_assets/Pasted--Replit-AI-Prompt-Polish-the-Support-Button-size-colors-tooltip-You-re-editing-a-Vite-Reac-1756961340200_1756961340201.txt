

Replit AI Prompt — Polish the Support Button (size, colors, tooltip)

You’re editing a Vite + React + Tailwind + Radix project. The Support button is already fixed at the bottom-left and opens the existing modal. Apply the following UX polish:

Tasks
	1.	Tweak size & colors in client/src/components/support/SupportButton.tsx
	•	Make it a smaller pill and use neutral tones that fit the UI.
	•	Replace the button classes with:

className="fixed left-4 bottom-4 z-50 rounded-full px-3 py-1.5 shadow bg-neutral-800 text-white hover:bg-neutral-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-neutral-400"

	•	Keep aria-label="Open technical support".

	2.	Add a tooltip using @radix-ui/react-tooltip around the button.
	•	Wrap the button with a Radix Tooltip that shows “Contact Technical Support” on hover/focus.
	•	Example shape (adapt to file’s structure/import style):

import * as Tooltip from "@radix-ui/react-tooltip";

export function SupportButton() {
  const [open, setOpen] = useState(false);

  return (
    <>
      <Tooltip.Provider delayDuration={200} skipDelayDuration={400}>
        <Tooltip.Root>
          <Tooltip.Trigger asChild>
            <button
              onClick={() => setOpen(true)}
              aria-label="Open technical support"
              className="fixed left-4 bottom-4 z-50 rounded-full px-3 py-1.5 shadow bg-neutral-800 text-white hover:bg-neutral-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-neutral-400"
            >
              Support
            </button>
          </Tooltip.Trigger>
          <Tooltip.Portal>
            <Tooltip.Content
              side="top"
              align="start"
              className="select-none rounded-md bg-neutral-900 px-2 py-1 text-xs text-white shadow"
            >
              Contact Technical Support
              <Tooltip.Arrow className="fill-neutral-900" />
            </Tooltip.Content>
          </Tooltip.Portal>
        </Tooltip.Root>
      </Tooltip.Provider>

      <SupportModal open={open} onClose={() => setOpen(false)} />
    </>
  );
}


	3.	Keep global mounting
	•	Ensure <SupportButton /> remains mounted once at the app root (e.g., client/src/App.tsx) so it appears on all pages and stays fixed to the viewport.

Acceptance Criteria
	•	Button remains fixed bottom-left, but is visually smaller and less intrusive.
	•	Neutral palette: bg-neutral-800 → hover:bg-neutral-700.
	•	Accessible focus ring on keyboard focus.
	•	Tooltip appears on hover/focus with the text “Contact Technical Support”.
	•	No regressions to the existing modal open/close behavior.