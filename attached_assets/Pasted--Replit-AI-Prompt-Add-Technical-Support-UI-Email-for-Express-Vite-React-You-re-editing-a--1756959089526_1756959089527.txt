
Replit AI Prompt — Add “Technical Support” (UI + Email) for Express + Vite React

You’re editing a Vite + React app with Tailwind + shadcn/Radix UI (global CSS at client/src/index.css) and a TypeScript Express backend started from server/index.ts. Email is sent via @sendgrid/mail which is already in dependencies. Implement a floating “Support” button (bottom-left), open a modal with Title and Description, and on submit send an email to:
abdullah@ambersand.ai and rakan@ambersand.ai.
Use TypeScript throughout.

Repo assumptions (verify & follow)
	•	Frontend: Vite React app, Tailwind + shadcn/Radix (CSS at client/src/index.css).  
	•	Backend: Express TypeScript entry at server/index.ts (npm run dev uses tsx server/index.ts).  
	•	Email: Use @sendgrid/mail (already installed). There’s a local email-test.js testing SendGrid keys. Reuse that library.    

What to build

1) Frontend (React + Tailwind + Radix)
	•	Create a floating Support button visible on every page:
	•	File: client/src/components/support/SupportButton.tsx
	•	Fixed bottom-left: fixed left-4 bottom-4 z-50 rounded-full px-4 py-2 shadow bg-black/80 text-white hover:bg-black
	•	aria-label="Open technical support"
	•	Clicking opens a Support Modal.
	•	Create a Support Modal using Radix Dialog:
	•	File: client/src/components/support/SupportModal.tsx
	•	Fields & validation:
	•	Title: required, 3–120 chars.
	•	Description: required, 10–4000 chars, multiline.
	•	Buttons: Cancel, Send (disabled while sending).
	•	Keyboard: ESC closes; prevent Enter from submitting when focus is in Title; allow Cmd/Ctrl+Enter to submit when focus is in Description.
	•	Toast success/error (Radix Toast or a tiny custom hook).
	•	POST JSON to backend /api/support with { title, description, path: window.location.pathname }.
	•	Mount the button globally so it appears across the app:
	•	Add <SupportButton /> inside the root layout or client/src/App.tsx (or the top-level route component used by wouter). (The project uses wouter, so ensure it doesn’t get hidden per-route.)  

Minimal TS snippet (shape & classes; the agent should flesh out imports/types):

// client/src/components/support/SupportButton.tsx
import { useState } from "react";
import { SupportModal } from "./SupportModal";

export function SupportButton() {
  const [open, setOpen] = useState(false);
  return (
    <>
      <button
        onClick={() => setOpen(true)}
        aria-label="Open technical support"
        className="fixed left-4 bottom-4 z-50 rounded-full px-4 py-2 shadow bg-black/80 text-white hover:bg-black"
      >
        Support
      </button>
      <SupportModal open={open} onClose={() => setOpen(false)} />
    </>
  );
}

// client/src/components/support/SupportModal.tsx
import * as Dialog from "@radix-ui/react-dialog";
import { useState } from "react";

export function SupportModal({ open, onClose }: { open: boolean; onClose: () => void }) {
  const [title, setTitle] = useState("");
  const [desc, setDesc] = useState("");
  const [loading, setLoading] = useState(false);

  async function submit() {
    const t = title.trim(), d = desc.trim();
    if (t.length < 3 || t.length > 120) return alert("Title must be 3–120 chars.");
    if (d.length < 10 || d.length > 4000) return alert("Description must be 10–4000 chars.");
    setLoading(true);
    try {
      const res = await fetch("/api/support", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ title: t, description: d, path: window.location.pathname }),
      });
      if (!res.ok) throw new Error(await res.text());
      alert("Support request sent.");
      onClose();
      setTitle(""); setDesc("");
    } catch (e) {
      alert("Failed to send. Please try again.");
    } finally {
      setLoading(false);
    }
  }

  if (!open) return null;
  return (
    <Dialog.Root open={open} onOpenChange={(v) => !v && onClose()}>
      <Dialog.Portal>
        <Dialog.Overlay className="fixed inset-0 bg-black/40" />
        <Dialog.Content className="fixed left-1/2 top-1/2 z-50 w-[92vw] max-w-lg -translate-x-1/2 -translate-y-1/2 rounded-xl bg-white p-6 shadow">
          <Dialog.Title className="text-lg font-semibold">Contact Technical Support</Dialog.Title>
          <div className="mt-4 space-y-3">
            <input
              className="w-full rounded border p-2"
              placeholder="Short summary (e.g., Cannot upload evidence)"
              value={title}
              onChange={(e) => setTitle(e.target.value)}
            />
            <textarea
              className="w-full min-h-[140px] rounded border p-2"
              placeholder="Describe the issue, steps to reproduce, IDs, etc."
              value={desc}
              onChange={(e) => setDesc(e.target.value)}
            />
          </div>
          <div className="mt-4 flex justify-end gap-2">
            <button className="rounded border px-4 py-2" onClick={onClose} disabled={loading}>Cancel</button>
            <button className="rounded bg-black px-4 py-2 text-white disabled:opacity-50" onClick={submit} disabled={loading}>
              {loading ? "Sending..." : "Send"}
            </button>
          </div>
        </Dialog.Content>
      </Dialog.Portal>
    </Dialog.Root>
  );
}

2) Backend (Express + @sendgrid/mail)
	•	Add a POST route POST /api/support that:
	•	Validates body: { title, description, path? } with the same limits.
	•	Sends an email via @sendgrid/mail to both abdullah@ambersand.ai and rakan@ambersand.ai.
	•	Subject: "[Ambersand Support] {title}".
	•	HTML body includes Title, Description, Path, and ISO timestamp.
	•	File: server/routes/support.ts (new), and mount in server/index.ts as /api/support.
	•	Use env vars:
	•	SENDGRID_API_KEY
	•	SENDGRID_FROM_EMAIL (use a verified identity in SendGrid; a test script already exists in email-test.js).  

Minimal TS snippet (shape; wire into server/index.ts):

// server/routes/support.ts
import { Router } from "express";
import sgMail from "@sendgrid/mail";

const router = Router();

sgMail.setApiKey(process.env.SENDGRID_API_KEY || "");

router.post("/", async (req, res) => {
  try {
    const { title, description, path } = req.body ?? {};
    const t = String(title ?? "").trim();
    const d = String(description ?? "").trim();

    if (t.length < 3 || t.length > 120) return res.status(400).json({ error: "Invalid title" });
    if (d.length < 10 || d.length > 4000) return res.status(400).json({ error: "Invalid description" });

    const tos = ["abdullah@ambersand.ai", "rakan@ambersand.ai"];
    const from = process.env.SENDGRID_FROM_EMAIL;
    if (!process.env.SENDGRID_API_KEY || !from) {
      return res.status(500).json({ error: "Email not configured" });
    }

    await sgMail.send({
      to: tos,
      from: { email: from, name: "Ambersand Support" },
      subject: `[Ambersand Support] ${t}`,
      html: `
        <p><strong>Title:</strong> ${t}</p>
        <p><strong>Description:</strong><br/>${d.replace(/\n/g, "<br/>")}</p>
        <p><strong>Page:</strong> ${path || "N/A"}</p>
        <p><strong>At:</strong> ${new Date().toISOString()}</p>
      `,
    });

    res.json({ status: "ok" });
  } catch (e) {
    res.status(502).json({ error: "Failed to send email" });
  }
});

export default router;

// server/index.ts (mount the route)
import express from "express";
import supportRouter from "./routes/support";
const app = express();
app.use(express.json());
app.use("/api/support", supportRouter);
// ... keep existing routes & server listen as-is

3) Env & config
	•	Add to Replit Secrets / .env:
	•	SENDGRID_API_KEY=...
	•	SENDGRID_FROM_EMAIL=admin@ambersand.ai (or whichever identity is verified in SendGrid—use the same sender you validated via the included email-test.js).  
	•	No package install needed for SendGrid (already present).  

4) Acceptance criteria
	•	Floating Support button appears bottom-left on all pages.
	•	Modal opens, validates inputs, closes on success.
	•	POST /api/support returns 200 and triggers an email to both recipients.
	•	Handles misconfig (missing envs) with clear 500 JSON error.
	•	TypeScript builds cleanly; dev server still starts via tsx server/index.ts.  

