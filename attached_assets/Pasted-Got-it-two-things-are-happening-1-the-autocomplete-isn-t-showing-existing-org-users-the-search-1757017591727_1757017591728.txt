Got it — two things are happening:
	1.	the autocomplete isn’t showing existing org users (the search call isn’t returning results to the UI), and
	2.	the invite path “succeeds” but no email is actually sent (mailer is misconfigured or errors are swallowed).

Below is a single, copy-paste Replit AI prompt that fixes both sides: the client search (so the dropdown shows users) and the backend invite (so emails actually send or fail loudly).

⸻

Replit AI Prompt — Fix Smart Assignee Search + Invite Email

You’re editing a Vite + React + Tailwind + shadcn client and an Express + TypeScript + Drizzle backend. The “Smart Assignee” should:
	•	live-search org users while typing (dropdown shows results),
	•	allow inviting a brand-new email (sends email),
	•	never silently “succeed” when the mailer is misconfigured.

1) Client: make user search authenticated, debounced, and actually render results

File: client/src/components/users/AssigneeSmartInput.tsx
	•	Use React Query with a stable queryKey, pass credentials (or reuse your apiRequest), and only fire when the popover is open and query length ≥ 2.
	•	Render the list; when it’s empty but the input is a valid email, show an Invite row.

// Imports assumed: useState, useEffect, useMemo from react; useQuery from @tanstack/react-query;
// shadcn/Radix combobox/command components; your `apiRequest` helper if available.

const EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

export default function AssigneeSmartInput({ onResolve, disabled }: {
  onResolve: (r:
    | { type: "existing"; userId: string; email: string; name?: string }
    | { type: "invite"; inviteId: number; email: string }
  ) => void;
  disabled?: boolean;
}) {
  const [open, setOpen] = useState(false);
  const [query, setQuery] = useState("");

  const enabled = open && query.trim().length >= 2;

  const fetchUsers = async () => {
    const q = query.trim();
    if (q.length < 2) return { items: [] };
    // EITHER use your apiRequest helper (recommended)…
    // return apiRequest(`/api/users/search?q=${encodeURIComponent(q)}&limit=8`, 'GET');

    // …OR raw fetch WITH CREDENTIALS so cookies are sent in dev:
    const res = await fetch(`/api/users/search?q=${encodeURIComponent(q)}&limit=8`, {
      method: "GET",
      credentials: "include",   // ← IMPORTANT
      headers: { "Accept": "application/json" },
    });
    if (!res.ok) return { items: [] };
    return res.json();
  };

  const { data, isFetching } = useQuery({
    queryKey: ["users-search", query], // stable key that changes only with query
    queryFn: fetchUsers,
    enabled,
    staleTime: 30_000,
  });

  const items: Array<{ id: string; email: string; name?: string; avatarUrl?: string | null }> =
    (data?.items ?? []).filter(Boolean);

  const canInvite = EMAIL_RE.test(query.trim()) && !items.some(u => u.email.toLowerCase() === query.trim().toLowerCase());

  async function invite(email: string) {
    const res = await fetch("/api/users/invite", {
      method: "POST",
      credentials: "include",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ email }),
    });
    if (res.status === 409) {
      const { userId } = await res.json();
      onResolve({ type: "existing", userId, email });
      return;
    }
    if (!res.ok) {
      const err = await res.text();
      alert(`Failed to send invite: ${err || res.status}`);
      return;
    }
    const { inviteId } = await res.json();
    onResolve({ type: "invite", inviteId, email });
  }

  return (
    <div className="relative w-full">
      <div className="flex items-center gap-2">
        <input
          className="w-full rounded border p-2"
          placeholder="Type a name or email…"
          value={query}
          onChange={e => setQuery(e.target.value)}
          onFocus={() => setOpen(true)}
          onBlur={() => setTimeout(() => setOpen(false), 150)}
          disabled={disabled}
          aria-autocomplete="list"
          aria-expanded={open}
        />
      </div>

      {open && (
        <div className="absolute z-50 mt-1 w-full rounded-md border bg-white shadow">
          {isFetching && <div className="p-2 text-sm text-neutral-500">Searching…</div>}

          {!isFetching && items.length > 0 && (
            <ul className="max-h-64 overflow-auto py-1">
              {items.map(u => (
                <li
                  key={u.id}
                  className="cursor-pointer px-3 py-2 hover:bg-neutral-50"
                  onMouseDown={(e) => e.preventDefault()}
                  onClick={() => onResolve({ type: "existing", userId: u.id, email: u.email, name: u.name })}
                >
                  <div className="text-sm font-medium">{u.name || u.email}</div>
                  <div className="text-xs text-neutral-500">{u.email}</div>
                </li>
              ))}
            </ul>
          )}

          {!isFetching && items.length === 0 && !canInvite && (
            <div className="p-2 text-sm text-neutral-500">No users found. Keep typing…</div>
          )}

          {!isFetching && canInvite && (
            <button
              type="button"
              className="flex w-full items-center justify-between px-3 py-2 text-left hover:bg-neutral-50"
              onMouseDown={(e) => e.preventDefault()}
              onClick={() => invite(query.trim())}
            >
              <span className="text-sm">
                Invite <span className="font-semibold">{query.trim()}</span>
              </span>
              <span className="text-xs text-neutral-500">Send email invite</span>
            </button>
          )}
        </div>
      )}
    </div>
  );
}

Also ensure in the Task form you send ONE of the two:
	•	Existing user → set assigneeId (string) and clear assigneeEmail.
	•	Invite → clear assigneeId and set assigneeEmail (email).
Don’t send both, or the server will prefer assigneeId and skip invite logic.

⸻

2) Server: make search actually find users in the org

File: server/users.ts (router mounted at /api/users)
	•	Keep id as string. Search both email and name; scope by org; limit results.
	•	Return { items: [...] }.

router.get("/search", isAuthenticated, async (req: any, res) => {
  const q = String(req.query.q || "").trim();
  const limit = Math.min(Number(req.query.limit || 8), 25);

  if (!q || q.length < 2) return res.json({ items: [] });
  if (!req.user?.claims?.org) return res.status(400).json({ items: [], message: "Organization missing" });

  const rows = await db.query.users.findMany({
    where: (u, { and, eq, or, ilike }) =>
      and(
        eq(u.organizationId, req.user.claims.org),
        or(
          ilike(u.email, `%${q}%`),
          ilike(u.name, `%${q}%`)
        )
      ),
    limit,
    columns: { id: true, email: true, name: true, avatarUrl: true }
  });

  const items = rows.map(u => ({
    id: u.id,                       // keep string
    email: u.email || "",
    name: u.name || u.email || "User",
    avatarUrl: u.avatarUrl || null,
  }));

  res.json({ items });
});


⸻

3) Server: make invites send (or fail loudly)

File: server/users.ts (invite route)
	•	Require org; normalize email; if user exists, return 409 with userId.
	•	Do not swallow SendGrid errors; if sending fails, return 500.
	•	Require SENDGRID_API_KEY and SENDGRID_FROM_EMAIL.

router.post("/invite", isAuthenticated, async (req: any, res) => {
  const email = String(req.body?.email || "").trim().toLowerCase();
  if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
    return res.status(400).json({ message: "Invalid email" });
  }
  if (!req.user?.claims?.org) {
    return res.status(400).json({ message: "Organization missing" });
  }

  const existing = await db.query.users.findFirst({
    where: (u, { and, eq }) => and(eq(u.organizationId, req.user.claims.org), eq(u.email, email)),
    columns: { id: true }
  });
  if (existing) return res.status(409).json({ message: "Already a user", userId: existing.id });

  const token = crypto.randomUUID().replace(/-/g, "");
  const [invite] = await db.insert(userInvites).values({
    organizationId: req.user.claims.org,
    email,
    role: "member",
    token,
  }).returning();

  // Mailer preflight
  if (!process.env.SENDGRID_API_KEY || !process.env.SENDGRID_FROM_EMAIL) {
    return res.status(500).json({ message: "Email not configured (SENDGRID_API_KEY / SENDGRID_FROM_EMAIL)" });
  }

  const acceptUrl = `${process.env.APP_BASE_URL || "http://localhost:3000"}/accept-invite?token=${token}`;

  try {
    const result = await emailService.sendEmail({
      to: email,
      subject: "You're invited to Ambersand",
      html: `<p>You’ve been invited to Ambersand.</p>
             <p><a href="${acceptUrl}">Accept your invite</a></p>`,
    });
    if (!result?.success) {
      return res.status(502).json({ message: `Failed to send invite email: ${result?.error || "unknown"}` });
    }
  } catch (e: any) {
    console.error("Invite email error:", e?.response?.body || e?.message || e);
    return res.status(502).json({ message: "Failed to send invite email" });
  }

  res.status(201).json({ inviteId: invite.id });
});

Also harden your mailer (server/emailService.ts) to require a verified sender and log SendGrid responses:

if (!process.env.SENDGRID_API_KEY) throw new Error("SENDGRID_API_KEY missing");
if (!process.env.SENDGRID_FROM_EMAIL) throw new Error("SENDGRID_FROM_EMAIL missing (must be verified)");


⸻

4) DB model: organization required on invites

File: shared/invites.ts
	•	Ensure:

organizationId: varchar("organization_id", { length: 64 }).notNull(),

	•	If the column is nullable in the DB, run a migration to set NOT NULL and backfill any blanks (or delete invalid rows).

⸻

5) One-pager for you to verify quickly
	•	In DevTools → Network:
	•	Type at least 2 chars in the assignee field → GET /api/users/search?q=xx returns 200 with { items: [...] }.
	•	Pick a user → the form sets assigneeId string.
	•	Type a valid new email → click Invite → POST /api/users/invite returns:
	•	409 { userId } if already a user (then we auto-switch to existing), or
	•	201 { inviteId } and you should also see NO 500 mailer errors in the server logs.
	•	If you still don’t get the invite email:
	•	Confirm env vars:
	•	SENDGRID_API_KEY
	•	SENDGRID_FROM_EMAIL (a verified sender in SendGrid)
	•	Optional: SENDGRID_FROM_NAME
	•	APP_BASE_URL (for the link)
	•	Check server logs now that we fail loudly; fix whatever error string it prints.

⸻

Why your dropdown was empty
	•	The search request wasn’t authenticated (no cookies), so the API treated it as unauthenticated and returned nothing. We fixed it by including credentials: "include" / using apiRequest.

Why invites “worked” but no email arrived
	•	The code likely swallowed SendGrid errors or tried to send from an unverified sender. We now hard-require the envs and return 500/502 if sending fails, with logs.

Apply this prompt and test again. If it still misbehaves, tell me the status + JSON for:
	•	GET /api/users/search?q=…
	•	POST /api/users/invite
and the server log line after clicking Invite — I’ll zero in on the exact line to tweak.